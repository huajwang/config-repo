server:
  port: 9004

spring:
  datasource:
    driver-class-name: "com.mysql.jdbc.Driver"
    url: "jdbc:mysql://localhost:3306/micro-cart"
    username: "root"
    password: "Gold1Silver2"
    initialization-mode: never
  jpa:
    database-platform: "org.hibernate.dialect.MySQL5Dialect"
    hibernate:
      ddl-auto: none
